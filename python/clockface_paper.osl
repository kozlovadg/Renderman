shader clockface_paper (
	color Cin=1,
output	color Cout=1
)
{
    point d=transform("polar",P);
    float s=(0.5 + d[0] * cos(d[1])) * 1;
    float t=(acos(d[2])/M_PI - 0.325) * 3;
    Cout=(color)texture("clockface.tx",s,t);
}

