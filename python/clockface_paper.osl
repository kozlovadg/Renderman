shader clockface_paper (
	color Cin=1,
output	color Cout=1
)
{
    float rot = radians(45);
    point d1 = rotate(P, rot, point(0,0,0), point(0,1,0));
    float s=(0.5 + d1[0] * cos(d1[1]));
    float t=(acos(d1[2])/M_PI - 0.325) * 3;
    Cout=(color)texture("clockface.tx",s,t);
}

